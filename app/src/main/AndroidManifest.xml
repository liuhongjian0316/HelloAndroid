<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="work.aijiu.helloandroid">



    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloAndroid">

        <activity
            android:name=".ui.activity.welcome.WelcomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.main.MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".ui.title_bar.SearchTitleBarActivity">
        </activity>
        <activity android:name=".ui.title_bar.SearchDetailActivity"></activity>

        <!--组件选择页面-->
        <activity android:name=".ui.activity.component.ComponentActivity"/>
        <!--TextView组件界面-->
        <activity android:name=".ui.activity.component.textview.TextViewActivity"/>
    </application>

</manifest>